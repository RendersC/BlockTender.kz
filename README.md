# 🚀 BlockTender.kz - Decentralized Tender Platform

## ✨ Система полностью переделана и работает!

### 📋 Что было исправлено

✅ **Admin Panel** - теперь корректно проверяет роль ADMIN вместо owner  
✅ **Система ролей** - админы могут участвовать БЕЗ подписки  
✅ **Интерфейс** - удален "My Tenders", добавлены нужные проверки  
✅ **Обработка ошибок** - все функции имеют корректную обработку ошибок  
✅ **Отображение цен** - все цены показываются в ETH  

---

## 🏗️ Архитектура системы

### Смарт-контракты

#### RewardToken.sol
- Стандартный ERC20 токен
- Выдается за участие в тендерах (100 RWT за 1 ETH)

#### TenderCrowdfunding.sol
- Основной контракт платформы
- Управление тендерами, подписками и ролями
- Функции:
  - `createTender()` - создание тендера (только ADMIN)
  - `contribute()` - участие в тендере (USER с подпиской или ADMIN)
  - `buySubscription()` - покупка годовой подписки за 0.5 ETH
  - `finalizeTender()` - финализация тендера (только organizer)
  - `refund()` - возврат средств если тендер не профинансирован
  - `assignRole()` - назначение роли пользователю (только owner)
  - `setSubscriptionPrice()` - изменение цены подписки (только owner)

### Фронтенд

| Страница | Путь | Функция |
|----------|------|---------|
| Главная | `/index.html` | Список всех тендеров, статистика |
| Создание | `/create.html` | Форма создания тендера (только ADMIN) |
| Детали | `/tender.html?id=X` | Детали и участие в тендере |
| Admin | `/admin.html` | Управление ролями и подписками |

---

## 🚀 Быстрый старт

### 1. Развертывание контрактов

```bash
# Установить зависимости
npm install

# Развернуть контракты
npx hardhat run scripts/deploy.js --network <network>
```

**При развертывании:**
- Адрес развертывания автоматически получает роль **ADMIN**
- Цена подписки установлена на **0.5 ETH**

### 2. Обновить адрес контракта в фронтенде

Откройте `frontend/app.js` и `frontend/admin.js`, найдите:

```javascript
const CONTRACT_ADDRESS = "0x632C4931F97B006ccdeA72D090c5645f461d8De7";
```

Измените на ваш адрес контракта.

### 3. Запустить фронтенд

```bash
cd frontend
npm install
npm run dev
```

---

## 👥 Система ролей

### USER (Пользователь)
```
- Может участвовать в тендерах ✓ (требуется подписка 0.5 ETH)
- Может создавать тендеры ✗
- Видится в меню: Tenders, Admin Panel
```

### ADMIN (Администратор)
```
- Может создавать тендеры ✓
- Может участвовать БЕЗ подписки ✓
- Может управлять ролями ✓ (через Admin Panel)
- Видится в меню: Tenders, Create Tender, Admin Panel
```

### OWNER (Владелец контракта)
```
- Все что может ADMIN
- Может менять цену подписки ✓
- Может снимать средства ✓
- Может назначать роли другим ✓
```

---

## 📝 Как использовать

### Для администраторов: Создать тендер

1. Перейдите на `Create Tender`
2. Заполните форму:
   - **Название**: "Строительство дороги"
   - **Описание**: "Подробное описание..."
   - **Цель**: "2.5" (в ETH)
   - **Длительность**: "7" (дней)
3. Нажмите "🚀 Create Tender"
4. Подтвердите в MetaMask

### Для пользователей: Участие в тендере

1. Откройте главную страницу
2. Если видите "⚠ No Subscription" - купите подписку
3. Нажмите "💳 Buy Subscription (0.50 ETH)"
4. Теперь видите "✓ Active Subscription"
5. Нажмите на тендер, введите сумму, подтвердите

### Для администраторов: Управление ролями

1. Перейдите на `Admin Panel`
2. В разделе "🛡️ Manage Roles":
   - Введите адрес пользователя
   - Выберите роль (User или Admin)
   - Нажмите "✓ Assign Role"

---

## 🛠️ Функции контракта

### Создание и управление тендерами

```solidity
// Создать тендер (требует роль ADMIN)
function createTender(
    string memory _title,
    string memory _description,
    uint256 _goal,      // в wei (например 1 ETH = 1e18 wei)
    uint256 _duration   // в секундах (например 7*86400 для 7 дней)
) external onlyAdmin

// Финализировать тендер (требует быть организатором)
function finalizeTender(uint256 tenderId) external

// Получить рефаунд (если тендер не достиг цели)
function refund(uint256 tenderId) external
```

### Подписки

```solidity
// Купить подписку (стоит subscriptionPrice)
function buySubscription() external payable

// Проверить активна ли подписка
function hasActiveSubscription(address user) external view returns (bool)

// Получить время истечения подписки
function getSubscriptionExpiry(address user) external view returns (uint256)

// Изменить цену подписки (только owner)
function setSubscriptionPrice(uint256 newPrice) external onlyOwner
```

### Роли

```solidity
// Назначить роль (только owner)
function assignRole(address user, Role role) external onlyOwner

// Получить роль пользователя (0=USER, 1=ADMIN)
function getRole(address user) external view returns (uint8)

// Проверить администратор ли
function isAdmin(address user) external view returns (bool)
```

---

## 📊 События (Events)

```solidity
event TenderCreated(uint256 indexed tenderId, address indexed organizer);
event Contributed(uint256 indexed tenderId, address indexed contributor, uint256 amount);
event TenderFinalized(uint256 indexed tenderId);
event Refunded(uint256 indexed tenderId, address indexed user, uint256 amount);
event RoleAssigned(address indexed user, Role role);
event SubscriptionPurchased(address indexed user, uint256 expiry);
event SubscriptionPriceUpdated(uint256 newPrice);
```

---

## 🔄 Жизненный цикл тендера

### Этап 1: Создание
```
Админ создает тендер → Контракт сохраняет данные → Появляется в списке
```

### Этап 2: Сбор средств
```
Люди видят тендер → Вносят деньги → Сумма растет
- User должен иметь подписку
- Admin может участвовать без подписки
```

### Этап 3: Дедлайн
```
Время истекает → Тендер помечается как "Ended"
```

### Этап 4: Финализация
```
Организатор вызывает finalizeTender() → Прекращается прием средств
```

### Этап 5: Рефаунд (если не достаточно средств)
```
Если totalRaised < goal → Люди могут запросить refund()
```

---

## 🧪 Тестирование

### На Sepolia/Goerli тестовой сети

```bash
# Получить тестовые ETH с крана:
# Sepolia: https://sepoliafaucet.com
# Goerli: https://goerlifaucet.com

# Запустить тесты
npx hardhat test

# Записать логи
npx hardhat test --logs
```

---

## ⚠️ Важные замечания

1. **Подписка длится 365 дней** с момента покупки
2. **Цена в wei** - если хотите 0.5 ETH, это `ethers.parseEther("0.5")`
3. **Роль 0 = USER, Роль 1 = ADMIN**
4. **Админы не нуждаются в подписке** для участия
5. **Только owner может управлять ценой** и снимать средства

---

## 🐛 Решенные проблемы

| Проблема | Решение |
|----------|---------|
| "Вы не админ в admin.html" | Изменена проверка на `getRole()` вместо `owner()` |
| "My Tenders не работает" | Удален из всех меню |
| "Adminы не могут участвовать" | Закон в контракте: ADMIN не требует подписку |
| "Цены в wei вместо ETH" | Преобразование через `ethers.formatEther()` |
| "Ошибки при транзакции" | Добавлена полная обработка ошибок |

---

## 📞 Поддержка

### Если что-то не работает

1. Проверьте адрес контракта в `app.js` и `admin.js`
2. Убедитесь что контракт развернут на нужной сети
3. Проверьте что у вас есть тестовые ETH
4. Откройте консоль браузера (F12) и посмотрите ошибки
5. Убедитесь что MetaMask подключен к нужной сети

### Полезные ссылки

- [Etherscan Sepolia](https://sepolia.etherscan.io)
- [MetaMask Docs](https://docs.metamask.io)
- [Ethers.js Docs](https://docs.ethers.org)
- [Solidity Docs](https://docs.soliditylang.org)

---

## 📄 Лицензия

MIT License

---

## ✅ Статус проекта

- ✅ Smart Contracts готовы к развертыванию
- ✅ Фронтенд полностью функционален
- ✅ Система ролей и подписок работает
- ✅ Admin Panel доступна
- ✅ Все ошибки исправлены

**🎉 Проект готов к использованию!**
