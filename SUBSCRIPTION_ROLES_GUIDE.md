# BlockTender.kz - Subscription & Role System

## 🎯 Новые функции

### 1. **Система подписок (Subscription System)**
- **Цена подписки**: 0.50 ETH
- **Длительность**: 1 год
- Пользователи должны иметь активную подписку чтобы участвовать в тендерах
- Администратор может изменить цену подписки через Admin Panel

### 2. **Система ролей (Role System)**
Есть две роли:

#### **USER (Пользователь)**
- Может участвовать в тендерах если у них есть активная подписка
- Видит все доступные тендеры
- Может вносить деньги в тендеры
- Не может создавать новые тендеры

#### **ADMIN (Администратор)**
- Может создавать новые тендеры
- Может финализировать тендеры после истечения сроков
- Видит Create Tender кнопку в меню

### 3. **Admin Panel**
Доступен только владельцу контракта (owner). Позволяет:
- ✅ **Назначить роли** пользователям (USER или ADMIN)
- 🔍 **Проверить роль** любого пользователя
- 💳 **Изменить цену подписки**
- 🏦 **Снять все средства** из контракта

## 📋 Как использовать

### Развертывание (Deployment)

1. **Разверните контракт RewardToken:**
   ```bash
   npx hardhat run scripts/deploy.js --network <network-name>
   ```

2. **Разверните TenderCrowdfunding передав адрес RewardToken:**
   ```bash
   npx hardhat run scripts/deploy.js --network <network-name>
   ```
   - При развертывании владелец (owner) автоматически получает роль ADMIN
   - Сохраните адреса контрактов

3. **Обновите адреса в фронтенде:**
   - Отредактируйте `frontend/app.js` и `frontend/admin.js`
   - Измените `CONTRACT_ADDRESS` на адрес TenderCrowdfunding контракта

### Назначение ролей пользователям

1. Перейдите на страницу **Admin Panel** (`/frontend/admin.html`)
2. Подключитесь к MetaMask (как owner контракта)
3. Введите адрес пользователя
4. Выберите роль (User или Admin)
5. Нажмите "Assign Role"

### Покупка подписки

1. Обычные пользователи (USER) видят кнопку "Buy Subscription (0.50 ETH)"
2. Нажмите кнопку и подтвердите транзакцию в MetaMask
3. После успешной покупки подписка активна в течение 1 года

### Создание тендера (только для ADMIN)

1. Перейдите на страницу **Create Tender**
2. Заполните детали тендера:
   - Название
   - Описание
   - Цель финансирования (в ETH)
   - Длительность (в днях)
3. Нажмите "Create Tender"

### Участие в тендере

1. Пользователь должен иметь активную подписку
2. На главной странице нажмите "Participate"
3. Введите сумму для внесения (в ETH)
4. Подтвердите транзакцию

## 🔄 Сценарии использования

### Сценарий 1: Регистрация нового администратора
```
1. Администратор идет на Admin Panel
2. Вводит адрес нового администратора
3. Выбирает "Admin" из dropdown
4. Нажимает "Assign Role"
→ Новый администратор может теперь создавать тендеры
```

### Сценарий 2: Пользователь хочет участвовать в тендере
```
1. Пользователь подключает MetaMask
2. Видит уведомление "No Subscription" (красное)
3. Нажимает "Buy Subscription (0.50 ETH)"
4. Подтверждает платеж в MetaMask
5. Видит "Active Subscription" (зеленое)
6. Теперь может участвовать в тендерах
```

### Сценарий 3: Администратор хочет изменить цену подписки
```
1. Администратор идет на Admin Panel
2. Находит раздел "Subscription Settings"
3. Видит текущую цену
4. Вводит новую цену (в ETH)
5. Нажимает "Update Price"
6. Подтверждает в MetaMask
→ Новые подписки будут стоить новую цену
```

## 📊 Состояние пользователя

Каждый пользователь имеет:
- **Role**: 0 = USER, 1 = ADMIN
- **Subscription Expiry**: Timestamp когда истекает подписка (0 = нет подписки)

## 🔐 Функции контракта

### Публичные функции

```solidity
// Покупка подписки (платежная функция)
function buySubscription() external payable

// Проверка активной подписки
function hasActiveSubscription(address user) external view returns (bool)

// Получить время истечения подписки
function getSubscriptionExpiry(address user) external view returns (uint256)

// Назначить роль пользователю (только для owner)
function assignRole(address user, Role role) external onlyOwner

// Получить роль пользователя (0 = USER, 1 = ADMIN)
function getRole(address user) external view returns (uint8)

// Проверить является ли пользователь администратором
function isAdmin(address user) external view returns (bool)

// Создавать тендер (только для ADMIN)
function createTender(...) external onlyAdmin

// Участвовать в тендере (требует активную подписку)
function contribute(uint256 tenderId) external payable onlySubscribed

// Изменить цену подписки (только для owner)
function setSubscriptionPrice(uint256 newPrice) external onlyOwner

// Снять все средства (только для owner)
function withdrawFunds() external onlyOwner
```

## 📝 События (Events)

```solidity
event SubscriptionPurchased(address indexed user, uint256 expiry);
event RoleAssigned(address indexed user, Role role);
event SubscriptionPriceUpdated(uint256 newPrice);
```

## ⚠️ Важные замечания

1. **Только ONE контракт ADMIN при развертывании** - это адрес развертывания (owner)
2. **Подписка длится 365 дней** от момента покупки
3. **Цена подписки хранится в wei** (не забудьте преобразовывать)
4. **Любой может быть назначен администратором** через Admin Panel
5. **Средства подписок хранятся в контракте** - используйте `withdrawFunds()` для вывода

## 🚀 Примеры вызовов

### Покупить подписку через web3.js
```javascript
const contract = new ethers.Contract(ADDRESS, ABI, signer);
const price = await contract.subscriptionPrice();
const tx = await contract.buySubscription({ value: price });
await tx.wait();
```

### Назначить пользователя администратором
```javascript
const userAddress = "0x...";
const tx = await contract.assignRole(userAddress, 1); // 1 = ADMIN
await tx.wait();
```

### Проверить подписку пользователя
```javascript
const hasActive = await contract.hasActiveSubscription("0x...");
console.log(hasActive); // true/false
```

## 📞 Поддержка

Все новые функции полностью интегрированы с существующей системой тендеров BlockTender.kz
